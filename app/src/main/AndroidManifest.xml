<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mz.gerasoft.regulador_rodovia">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu_principal" />
        <activity android:name=".ConsultarMulta" />
        <activity android:name=".CadastrarMulta" />
        <activity android:name=".GerarRelatorio" />
        <activity android:name=".ConsultarArtigo" />
        <activity
            android:name=".main"
            android:label="@string/title_activity_main" />
        <activity android:name=".Comprovativo_multa" />
        <activity android:name=".cadastrarAgente" />
        <activity android:name=".alterarsenha" />
        <activity android:name=".contas" />
        <activity android:name=".pdf"></activity>
    </application>

</manifest>